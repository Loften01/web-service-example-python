services:
  app:
    environment:
      CONFIG_PATH: ${CONFIG_PATH}

    build:
      context: "."
      dockerfile: "docker/exchanger/Dockerfile"

    volumes:
      - ".:/app"

    ports:
      - "14200:14200"

    restart: "unless-stopped"
